# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# personal scratchpad — never committed
notes/

# secrets — defensive ignores. real secrets live in 1Password + GH secrets,
# but if someone accidentally drops a .p12 / .key / .pem into the repo, git
# will refuse to track it instead of leaking it to a PR.
*.p12
*.pem
*.cer
*.key
.tauri/
.env
.env.local
.env.*.local
*.pkcs12

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
marketing.md
.playwright-mcp/

# Local AI coding tools - never publish with the product
AGENTS.md
CLAUDE.md
.claude/
.codex/
GEMINI.md
.gemini/
.cursor/
.cursorrules
.windsurf/
.windsurfrules
.aider*
.opencode/
opencode.json
.github/copilot-instructions.md
.specstory/
.continue/
